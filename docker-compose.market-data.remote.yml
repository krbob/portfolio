services:
  portfolio-api:
    environment:
      PORTFOLIO_MARKET_DATA_ENABLED: "true"
      PORTFOLIO_STOCK_ANALYST_BASE_URL: "${PORTFOLIO_STOCK_ANALYST_BASE_URL:?set PORTFOLIO_STOCK_ANALYST_BASE_URL in .env or the shell}"
      PORTFOLIO_EDO_CALCULATOR_BASE_URL: "${PORTFOLIO_EDO_CALCULATOR_BASE_URL:?set PORTFOLIO_EDO_CALCULATOR_BASE_URL in .env or the shell}"
